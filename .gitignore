# binary
/tidefetch
web/node_modules/
site/node_modules/
site/dist/
.DS_Store

# go
*.test
*.out
# Anchored: GoReleaser output at the repo root only. An unanchored "dist/"
# also matches web/dist, whose built assets are committed on purpose because
# they are embedded into the binary by go:embed.
/dist/
.capture/
