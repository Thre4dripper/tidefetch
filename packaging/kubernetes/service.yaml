apiVersion: v1
kind: Service
metadata:
  name: tidefetch
  namespace: tidefetch
  labels:
    app.kubernetes.io/name: tidefetch
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tidefetch
  ports:
    - name: http
      port: 8210
      targetPort: http
      protocol: TCP